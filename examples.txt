program(num n){
    var fak = func(num n){
        if n == 0 then
            return 1;
        else
            return n * fak(n-1);
    };

    output fak(n);
}

program(num a, num b){
    var i = 0;

    for i to a in 1{
        b = b * b;
    };

    output b;
}