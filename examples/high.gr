program{
    var fhigh := func (var a, var b){
        var flow := func (var x){
            return a + b + x;
        };

        return flow;
    };

    var f;

    f := fhigh(1,1);
    return f(1);
}