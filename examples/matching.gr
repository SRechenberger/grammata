program ParserExample 
with
    var s := pair(a,b);
begin
    query nums(x) for X ?- 
        pair(X,Y) :=: x;
        pair(Y,X) :=: x
    end

    func main() 
    does
        return oneOf($s,nums($s)); 
    end
end
