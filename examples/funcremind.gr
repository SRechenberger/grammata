program FunctionalReminding
begin
	lambda save(x) is
		\$dummy . $x
	end

	lambda load() is
		(remind dummy)
	end

	proc main()
	with 
		var x;
	does
		keep save(100);

		x := load();

		return $x;
	end

end
