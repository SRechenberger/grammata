program IfThenElse
begin

    query bools() for X ?-
        X :=: true; 
        X :=: false
    end

    func main() does
        if bools() then
            return 1;
        else
            if bools() then 
                return 2;
            else 
                return 3;
            end
        end
    end
end