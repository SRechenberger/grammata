program{
    func fib := func(num n){
        if (n <= 1 || n <= 2) then {
            return 1;
        } else {
            return fib(n-1) + fib(n-2);
        };
    };

    
    return fib(10);
}
