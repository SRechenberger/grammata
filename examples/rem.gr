program RemTest
begin
	query foo() for X 
	?- X :=: 0; X :=: 1; X :=: 2; X :=: 3
	end

	proc main() 
	with
		var x;
	does
		keep nil;
		x := foo();
		if $x < 3 then
			keep ($x : remind);
			backtrack;
		else 
			return remind;
		end
	end
end
