program ParserExample 
    with
    begin

        lambda add(n) is
            $n + 1
        end

        base nums says
            p(add(5)).
            p(5).
        end

        query main() asks nums for X ?- p(X) end
    end
