program ParserExample 
    with
    begin

        lambda fak(n) is
            if $n <= 1 then 1 else $fak($n-1) end
        end

        base nums says
            p(fak(5)).
            p(125).
            p(88).
        end

        query main() asks nums for X ?- p(X) end
    end
