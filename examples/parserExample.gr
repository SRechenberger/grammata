program ParserExample 
with
    var s := dec(dec(2));
    var i;
begin
    query nums() for X ?- 
        X :=: 1; 
        X :=: 2; 
        X :=: s 
    end

    func main() 
    with
        var x := 0;
    does
        for i from 0 to nums do
            x := $x + 1;
        end
    end
end
