program {
    var o := 0; 
    var p := proc (var x) {
        o := x;        
    };

    var f := func (var x) {
        return x * x;
    };

    call p (100);
    call p (f(200)); ## ERROR could not enter scope "f"

    return o;
}